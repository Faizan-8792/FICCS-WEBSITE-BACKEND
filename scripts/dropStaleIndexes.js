/**
 * This script was previously used to drop stale MongoDB indexes.
 * With MySQL/Sequelize, indexes are managed via model definitions and sync().
 * This file is kept as a no-op placeholder for backward compatibility.
 */
console.log('No action needed — MySQL indexes are managed via Sequelize model definitions.');
process.exit(0);
